mixin input_description(object)
  div(class=`input_description ${object.blockMods}`)
    div(class=`input_description__container ${object.containerMods}`)
      p(class=`input_description__content ${object.contentMods}`) #{object.description}
